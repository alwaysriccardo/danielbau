export interface ServiceItem {
  id: number;
  number: string;
  title: string;
  subtitle: string;
  description: string;
  image: string;
  accentColor: string;
}

export interface NavLink {
  label: string;
  href: string;
}
